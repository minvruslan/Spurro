import type { SupportedProtocolFamily } from "../types/SupportedProtocolFamily"

export const SUPPORTED_PROTOCOL_FAMILIES: Record<SupportedProtocolFamily, { name: string }> = {
  amneziawg: { name: "AmneziaWG" },
}
