// Base directory for deploy files on nodes; each protocol gets a subdirectory inside it.
// Pinned into server.data.contract on first provisioning; already-provisioned nodes keep their value.
export const VPN_NODE_BASE_DIRECTORY = "/opt/spurro"
