// DNS servers written into newly created client configs on any node.
// Pinned into server.data.contract on first provisioning; already-provisioned nodes keep their value.
export const VPN_NODE_DNS = "1.1.1.1, 1.0.0.1"
